<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.shatokhin.lessonXML">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LessonXML"
        tools:targetApi="31">
        <activity
            android:name=".CalculatorOtherLayout"
            android:exported="false" />
        <activity
            android:name=".CalculatorConstraintLayout"
            android:exported="false" />
        <activity
            android:name=".MusicPlayerOtherLayout"
            android:exported="false" />
        <activity
            android:name=".MusicPlayerConstraintLayout"
            android:exported="false" />
        <activity
            android:name=".TelegramOtherLayout"
            android:exported="false" />
        <activity
            android:name=".TelegramConstraintLayout"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>